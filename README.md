## React Hooks and custom Widgets

HTML elements can be replaced with < React.Fragment > in order to not return the extra HTML element where key is involved
< React.Fragment key={item.title} >
</ React.Fragment>

### Hooks in React
